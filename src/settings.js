function controlls() {
    if (controllSettings == true) {
        push();
        rectMode(CENTER);
        fill(51, 128);
        rect(0, 0, width, height);
        pop();

        push();
        rectMode(CENTER)
        fill(51)
        rect(0, 0, 700, 450)
        pop();
    }
}